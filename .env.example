# Render / production (set in dashboard — do not commit real values)
PORT=8080
PREFLIGHT_PUBLIC_URL=https://preflight-mcp-relay.onrender.com
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID=price_...

# Render also sets RENDER_EXTERNAL_URL automatically (used as checkout URL fallback)

# Local dev: disable billing gates (everyone treated as Pro)
# PREFLIGHT_BILLING_ENFORCE=0
