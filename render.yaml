services:
  - type: web
    name: preflight-mcp-relay
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    plan: free
    envVars:
      - key: PORT
        value: "8080"
      # Stripe (required for Preflight Pro billing)
      # - key: STRIPE_SECRET_KEY
      # - key: STRIPE_WEBHOOK_SECRET
      # - key: STRIPE_PRICE_ID
      # - key: PREFLIGHT_PUBLIC_URL
      #   value: https://preflight-mcp-relay.onrender.com
